import { Col, Container, Row } from "react-bootstrap";

const SurfDisclamer = () => {
  return (
    <Container fluid>
      <Row className="justify-content-center text-white mt-5">
        <Col xs={12} md={8}>
          <h2>Search for the waves is the service dedicated to surfers looking for the best waves on the planet.</h2>
        </Col>
      </Row>
    </Container>
  );
};
export default SurfDisclamer;
